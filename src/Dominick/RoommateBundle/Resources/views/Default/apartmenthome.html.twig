{% extends "DominickRoommateBundle::layout.html.twig" %}

{% block title "Roommate Expense Tracker" %}

{% block content_header '' %}

{% block content %}
    <div class="row">
        <div class="col-md-12">
            <h3 id="{{ app.user.apartmentId }}">

                {{ apartment.nickname }} <p><small>{{ apartment.address1 }}, {{ apartment.address2 }}, {{ apartment.city }}, {{ apartment.state }} {{ apartment.zip }}</small></p>
            </h3>
        </div>
        <a href="{{ path('expense_new') }}">Create a new expense</a>
    </div>
{% endblock %}
